/* A 'for' loop will execute the number of times it is initialized to unless the condition is false, or unless the condition
is false then it will not begin executing. Counters are often decremented or incremented to maintain 'for' loop executions */

// Example
var ourArray = [];

for (var i = 0; i < 5; i++) {
  ourArray.push(i);
}

// Setup
var myArray = [];

// Only change code below this line.

for (var i = 1; i <= 5; i ++) {
    myArray.push(i)};
}