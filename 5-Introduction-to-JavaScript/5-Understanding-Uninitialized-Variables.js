/* A variable is considered "undefined" if there is no other value assigned to it. NaN will result if mathematical operations
are performed on it and 'undefined' will result if string operations are preformed on it. */

// Initialize these three variables
var a = 5;
var b = 10;
var c = "I am a";

// Do not change code below this line

a = a + 1;
b = b + 5;
c = c + " String!";
