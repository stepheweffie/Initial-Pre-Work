/* The 'parseInt()' function is a built in function that will convert a string to an integer unless input is not a string
representing a number */

function convertToInteger(str) {
  return parseInt(str)
}

convertToInteger("56");