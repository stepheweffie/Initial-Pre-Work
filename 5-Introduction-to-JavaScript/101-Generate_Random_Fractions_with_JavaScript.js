// The 'Math.random()' function will return a random decimal value between 0 inclusive and up to but not including 1

function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}