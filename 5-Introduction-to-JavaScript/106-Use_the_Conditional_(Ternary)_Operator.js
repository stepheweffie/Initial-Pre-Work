// An 'if / else' statement can be replaced by the ternary operator in one line also known as the conditional operator

function checkEqual(a, b) {
  return a == b ? true : false;
}

checkEqual(1, 2);