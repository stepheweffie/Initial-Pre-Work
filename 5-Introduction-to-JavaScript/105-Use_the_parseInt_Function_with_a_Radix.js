// The 'parseInt()' function takes an argument for the base of the number in the first input and will convert to base 10

function convertToInteger(str) {
  return parseInt(str, 2);
}

convertToInteger("10011");